version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html

hooks:
  BeforeInstall:
    - location: codedeploy/before.sh
      timeout: 300
      runas: root
  AfterInstall:
    - location: codedeploy/after.sh
      timeout: 300
      runas: root
